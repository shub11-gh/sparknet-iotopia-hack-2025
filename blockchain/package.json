{
  "name": "energy-trading-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for decentralized energy trading",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:mumbai": "hardhat run scripts/deploy.js --network polygonMumbai",
    "node": "hardhat node"
  },
  "devDependencies": {
    "hardhat": "^2.22.16",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5"
  }
}
